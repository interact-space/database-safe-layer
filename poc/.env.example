# LLM 路径：local(Ollama) or openai
LLM_MODE=local

# Ollama (OpenAI 兼容) 默认
LLM_BASE_URL=http://127.0.0.1:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=llama3:latest

# 如果使用 OpenAI，改为：
# LLM_MODE=openai
# LLM_BASE_URL=
# LLM_API_KEY=sk-xxxx
# LLM_MODEL=gpt-4o-mini

# DB：默认 SQLite
DB_URL=sqlite:///./poc_demo.db

# 选项 A: 开发环境常用 (SQLite)
# DATABASE_URL=sqlite:///./app.db

# 选项 B: 生产环境 (MySQL)
# DATABASE_URL=mysql+pymysql://admin:MySecretPass123@192.168.1.100:3306/prod_db

# 选项 C: 生产环境 (PostgreSQL)
# DATABASE_URL=postgresql://postgres:MySecretPass123@localhost:5432/my_app
# 确保这里是 postgresql:// 开头，并且包含正确的用户名、密码、主机地址

# OMOP 版本（写入审计）
OMOP_VERSION=OMOP1
